# 官机框架 3.6 插件开发文档

> 本文档按当前 `/www/wwwroot/bt.jujiao.net/3.6/` 框架源码整理，插件目录为 `plugin/`，插件管理页为 `admin/plugin.php?appid=你的appid`。

---

## 1. 插件运行机制

框架入口：`index.php`

消息流程：

```text
QQ 官方事件 / 代发入口
    ↓
index.php 解析事件、校验 appid、初始化上下文
    ↓
载入 function.php
    ↓
载入 bot.php
    ↓
遍历 plugin/*.php
    ↓
只执行 main.json 中已启用的插件
```

插件文件必须放在：

```text
plugin/插件名.php
```

启用配置在：

```text
main.json
```

示例：

```json
{
  "102753159": {
    "secret": "机器人密钥",
    "type": "正式",
    "plugin": {
      "菜单": true,
      "音乐系统": true
    }
  }
}
```

注意：

- 插件名就是文件名，不含 `.php` 后缀。
- 只有 `main.json` 中 `plugin.插件名 = true` 的插件会执行。
- 框架使用 `require_once` 载入插件，同一个请求中每个插件只载入一次。
- 插件报错会被框架捕获并写入日志，不会中断后续插件。
- 插件里不要 `echo` / `print` 输出内容，回复消息请使用框架函数。

---

## 2. 插件基础模板

```php
<?php
// plugin/示例插件.php

if (消息来源 != "群聊" && 消息来源 != "私聊") {
    return;
}

if (消息 == "ping") {
    文字("pong");
    return;
}
```

推荐规则：

- 每个命令处理完后 `return;`，避免继续执行后面的逻辑。
- 使用 `消息`、`消息来源` 等常量，不要写成 `$消息`。
- 判断相等用 `==` 或 `===`，不要写成赋值 `=`。

---

## 3. 全局常量

框架在执行插件前会定义以下常量。

| 常量 | 说明 |
| --- | --- |
| `消息来源` | 当前事件来源：`群聊`、`私聊`、`加群`、`退群`、`互动` |
| `消息ID` | 当前消息 ID，普通群聊/私聊消息可用 |
| `事件ID` | 事件 ID，加群/退群/互动等事件可用 |
| `消息` | 用户消息文本，框架会 `trim(content, "/ ")`，会去掉开头/结尾的 `/` 和空格 |
| `来源` | 群聊时为群 openid，私聊时为用户 openid，事件时为对应来源 ID |
| `用户` | 触发用户 openid |
| `appid` | 当前机器人 AppID |
| `secret` | 当前机器人 Secret |
| `type` | 环境：`正式` 或 `沙箱` |
| `plugin` | 当前 appid 的插件启用数组 |
| `raw` | 原始 QQ 事件数组 |

事件映射：

| QQ 事件 | 消息来源 | 说明 |
| --- | --- | --- |
| `GROUP_AT_MESSAGE_CREATE` | `群聊` | 群 @ 机器人消息 |
| `C2C_MESSAGE_CREATE` | `私聊` | 用户私聊机器人 |
| `GROUP_ADD_ROBOT` | `加群` | 机器人被添加到群 |
| `GROUP_DEL_ROBOT` | `退群` | 机器人被移出群 |
| `INTERACTION_CREATE` | `互动` | 按钮等互动回调 |

---

## 4. 命令判断

由于框架会对 `消息` 执行：

```php
trim($content, "/ ")
```

所以群聊里用户发送：

```text
/菜单
```

插件中收到的 `消息` 通常是：

```text
菜单
```

基础判断：

```php
if (消息 == "菜单") {
    文字("这是菜单");
    return;
}
```

前缀命令：

```php
if (前缀(消息, "echo ")) {
    $text = 前缀后(消息, "echo ");
    文字($text);
    return;
}
```

限制场景：

```php
if (消息来源 != "群聊") {
    return;
}
```

多个场景：

```php
if (!in_array(消息来源, ["群聊", "私聊"])) {
    return;
}
```

---

## 5. 发送消息 API

这些函数主要定义在 `bot.php`。

### 5.1 文字

```php
文字("要发送的内容");
```

说明：

- 群聊自动带 `msg_id` 回复。
- 私聊优先使用 `事件ID`，否则使用 `消息ID`。
- 加群、退群、互动事件使用 `event_id`。

### 5.2 图片

```php
图片("https://example.com/a.jpg");
图片("https://example.com/a.jpg", "图片说明");

$data = file_get_contents("/path/a.jpg");
图片($data, "本地图片");
```

说明：

- URL 会走 QQ 文件上传接口。
- 非 URL 内容会按二进制 base64 上传。

### 5.3 语音

```php
语音("https://example.com/a.mp3");
```

说明：

- 框架会调用 `silk()` 将 mp3 链接转 silk。
- 当前转换接口：`https://oiapi.net/API/Mp32Silk?url=`。

### 5.4 文件

```php
文件("https://example.com/a.pdf", "文件名.pdf");

$data = file_get_contents("/path/a.zip");
文件($data, "a.zip");
```

### 5.5 视频

```php
视频("https://example.com/a.mp4");
```

### 5.6 官方键盘按钮

```php
按钮("keyboard_id");
```

说明：

- `keyboard_id` 需要在 QQ 开放平台配置。

### 5.7 原生 Markdown

```php
原生MD("# 标题\n内容");
原生MD("# 标题\n内容", "keyboard_id");
```

### 5.8 自定义模板 Markdown

```php
发MD("template_id", [
    ["key" => "title", "values" => ["标题"]],
    ["key" => "content", "values" => ["内容"]]
]);

// 单个参数也支持
发MD("template_id", ["key" => "content", "values" => ["内容"]], "keyboard_id");
```

### 5.9 原生自定义按钮

`原生按钮($md, $rows)` 用于发送带 inline keyboard content 的 Markdown。

```php
$md = "# 按钮示例";

$rows = [[
    "buttons" => [[
        "id" => "demo_btn",
        "render_data" => [
            "label" => "点我",
            "visited_label" => "已点击",
            "style" => 1
        ],
        "action" => [
            "type" => 1,
            "permission" => ["type" => 2],
            "data" => "demo:click",
            "at_bot_show_channel_list" => true,
            "unsupport_tips" => "当前客户端不支持"
        ]
    ]]
]];

原生按钮($md, $rows);
```

处理按钮回调：

```php
if (消息来源 === "互动") {
    $btnData = raw["d"]["data"]["resolved"]["button_data"] ?? (raw["d"]["data"]["data"] ?? "");

    if ($btnData === "demo:click") {
        文字("你点击了按钮");
        return;
    }
}
```

### 5.10 Ark 卡片

文本列表卡：

```php
文卡(
    ["text" => "选项1", "url" => "https://example.com"],
    ["text" => "选项2"]
);
```

大图卡：

```php
大图("主标题", "副标题", "https://example.com/cover.jpg");
```

跳转卡：

```php
跳转卡("标题", "描述", "https://example.com/img.jpg", "https://example.com");
```

### 5.11 流式回复

```php
流式("第一段", "第二段", "最后一段");
```

说明：

- 当前实现走私聊接口 `/v2/users/{来源}/messages`，更适合私聊使用。

### 5.12 撤回消息

```php
撤回("消息ID");
```

说明：

- 支持 `群聊`、`私聊`。

### 5.13 底层 API 请求

```php
BOTAPI("/v2/groups/" . 来源 . "/messages", "POST", json_encode($data, JSON_UNESCAPED_UNICODE));
```

说明：

- 会自动按 `type` 选择正式/沙箱域名。
- 会自动带 `Authorization: QQBot access_token`。
- AccessToken 由 `BOT凭证()` 缓存到 `database/function/{appid}`。

---

## 6. 原生 MD 常用短代码

### 6.1 艾特

```md
<@用户ID>
<@all>
```

```php
原生MD("你好 <@" . 用户 . ">");
```

### 6.2 点击直发命令

```md
[签到](mqqapi://aio/inlinecmd?enter=true&command=签到)
```

### 6.3 点击确认命令

```md
[清空](mqqapi://aio/inlinecmd?command=清空&enter=false&reply=false)
```

### 6.4 普通链接

```md
[打开网站](https://example.com)
```

### 6.5 图片

```md
![标题 #宽度px #高度px](https://example.com/a.jpg)
```

示例：

```php
原生MD("![头像 #25px #25px](" . 头像(用户) . ")｜<@" . 用户 . ">");
```

注意：当前框架/页面偏好图片尺寸语法为“前宽后高”，如：`#90px #30px`。

---

## 7. 数据存储

函数定义在 `function.php`。

### 7.1 写入

```php
写("文件名", "键名", $值);
```

### 7.2 读取

```php
$value = 读("文件名", "键名", "默认值");
```

实际保存路径：

```text
database/文件名
```

示例：

```php
$points = 读("points", 用户, 0);
$points += 10;
写("points", 用户, $points);
文字("当前积分：" . $points);
```

说明：

- `写()` 会自动创建目录。
- 使用文件锁 `flock`，并发下相对安全。
- 文件内容是 JSON 对象。
- `$文件` 可以带目录，如 `签到/2026-04-25`。

---

## 8. 日志

```php
wlog("日志内容");
```

日志路径：

```text
Log/{appid}/YYYY-MM-DD.log
```

框架会自动记录发送动作：文字、图片、语音、文件、视频、Markdown、按钮等。

建议调试：

```php
wlog("收到消息：" . 消息);
wlog(json_encode(raw, JSON_UNESCAPED_UNICODE));
```

---

## 9. 辅助函数

### 9.1 HTTP 请求

```php
curl($url, $method, $headers, $params);
```

示例：

```php
$res = curl("https://api.example.com", "GET", [], "");
```

支持方法：`GET`、`POST`、`PUT`、`DELETE`。

### 9.2 前缀判断

```php
前缀("hello world", "hello");     // true
前缀后("echo 你好", "echo ");    // 你好
```

### 9.3 头像

```php
$url = 头像(用户);
```

返回 QQ 机器人头像接口 URL。

### 9.4 BOT 信息

```php
$info = BOT信息();
```

请求 `/users/@me`。

### 9.5 二维码

```php
$png = 二维码("https://example.com");
图片($png, "二维码");
```

### 9.6 Markdown 转 HTML

```php
$html = markdown转html("# 标题");
```

### 9.7 HTML 转图

```php
$img = HTML转图($html, ".container");
图片($img, "生成图片");
```

参数：

```php
HTML转图($html, $selector = ".container", $noCache = false, $quality = 100, $scale = 1.5, $format = "png")
```

### 9.8 邮箱

```php
邮箱("标题", "内容", "收件人@qq.com", "发件人@qq.com", "授权码");
```

### 9.9 域名大写

```php
$text = 域名大写("https://a.com/test");
```

会把常见域名后缀首字母大写。

---

## 10. 画布/GD 工具

`function/GD.php` 提供 `画布` 类。

```php
$gd = new 画布();
$img = $gd->创建(600, 300, "#FFFFFF");
$gd->文字($img, "你好", 24, 50, 80, "#000000", "/path/font.ttf");
$data = $gd->二进制输出($img);
$gd->销毁($img);
图片($data, "生成图");
```

常用方法：

| 方法 | 说明 |
| --- | --- |
| `创建($宽, $高, $hex)` | 创建画布 |
| `销毁($画布)` | 销毁资源 |
| `输出($画布)` | 直接输出 PNG |
| `二进制输出($画布)` | 返回 PNG 二进制 |
| `HEX转RGB($hex)` | HEX 转 RGB |
| `颜色($画布, $hex)` | 分配颜色 |
| `贴图(...)` | 将图片贴到画布 |
| `文字(...)` | 写文字 |
| `直线(...)` | 画线 |
| `矩形(...)` / `填充矩形(...)` | 矩形 |
| `圆(...)` / `填充圆(...)` | 圆形 |

---

## 11. 加群、退群、互动事件

### 11.1 加群欢迎

```php
<?php
if (消息来源 == "加群") {
    原生MD("感谢添加机器人！\n[查看菜单](mqqapi://aio/inlinecmd?enter=true&command=菜单)");
    return;
}
```

### 11.2 退群记录

```php
<?php
if (消息来源 == "退群") {
    wlog("机器人被移出群，操作用户：" . 用户);
    return;
}
```

### 11.3 互动按钮回调

```php
<?php
if (消息来源 == "互动") {
    $data = raw["d"]["data"]["resolved"]["button_data"] ?? (raw["d"]["data"]["data"] ?? "");

    if ($data == "my_plugin:ok") {
        文字("操作成功");
        return;
    }
}
```

---

## 12. 后台插件管理

后台入口：

```text
admin/plugin.php?appid=你的appid
```

功能：

- 查看已启用、未启用、全部插件
- 添加插件
- 启用插件
- 禁用插件
- 编辑插件源码
- 删除插件文件

对应接口：`admin/api/plugin.php`

| type | 说明 |
| --- | --- |
| `filelist` | 获取 `plugin/*.php` 插件列表 |
| `add` | 添加插件文件 |
| `delete` | 删除插件文件 |
| `read` | 读取插件源码 |
| `write` | 写入插件源码 |
| `list` | 获取指定 appid 已启用插件 |
| `open` | 启用插件 |
| `close` | 禁用插件 |

编辑器说明：

- “编辑插件”弹窗使用代码高亮编辑器。
- 支持 PHP 高亮、行号、自动换行、深色主题。
- 保存后写回：`plugin/插件名.php`。

注意：

- 后台删除插件会直接删除文件。
- 后台禁用插件会从 `main.json` 的 plugin 配置中移除。
- 在线编辑插件前建议先备份重要插件。

---

## 13. 完整示例

### 13.1 菜单插件

```php
<?php
if (!in_array(消息来源, ["群聊", "私聊"])) {
    return;
}

if (消息 == "菜单") {
    $md = "# 功能菜单\n\n";
    $md .= "<@" . 用户 . "> 你好！\n\n";
    $md .= "[签到](mqqapi://aio/inlinecmd?enter=true&command=签到)\n";
    $md .= "[积分](mqqapi://aio/inlinecmd?enter=true&command=积分)\n";
    $md .= "![头像 #25px #25px](" . 头像(用户) . ")";

    原生MD($md);
    return;
}
```

### 13.2 签到积分

```php
<?php
if (!in_array(消息来源, ["群聊", "私聊"])) {
    return;
}

if (消息 == "签到") {
    $today = date("Y-m-d");
    $last = 读("sign", 用户, "");

    if ($last == $today) {
        文字("今天已经签到过了");
        return;
    }

    $points = 读("points", 用户, 0);
    $points += 10;

    写("sign", 用户, $today);
    写("points", 用户, $points);

    文字("签到成功，当前积分：" . $points);
    return;
}

if (消息 == "积分") {
    $points = 读("points", 用户, 0);
    原生MD("<@" . 用户 . "> 当前积分：**" . $points . "**");
    return;
}
```

### 13.3 API 插件

```php
<?php
if (!in_array(消息来源, ["群聊", "私聊"])) {
    return;
}

if (消息 == "一言") {
    $res = curl("https://v1.hitokoto.cn/?encode=json", "GET", [], "");
    $json = json_decode($res, true);
    $text = $json["hitokoto"] ?? "接口返回异常";
    文字($text);
    return;
}
```

### 13.4 图片生成

```php
<?php
if (消息 != "生成图") {
    return;
}

$gd = new 画布();
$img = $gd->创建(600, 300, "#FFFFFF");
$gd->文字($img, "Hello 官机框架", 24, 50, 100, "#000000", __DIR__ . "/../font.ttf");
$data = $gd->二进制输出($img);
$gd->销毁($img);

图片($data, "生成完成");
return;
```

---

## 14. 常见问题

### Q1：插件不执行？

检查：

- 文件是否在 `plugin/` 目录。
- 文件名是否是 `插件名.php`。
- `main.json` 中该插件是否为 `true`。
- 插件开头是否因为 `消息来源` 判断直接 `return`。
- 查看 `Log/{appid}/YYYY-MM-DD.log` 是否有报错。

### Q2：群聊命令为什么不用判断 `/菜单`？

框架会执行：

```php
trim(content, "/ ")
```

所以 `/菜单` 到插件里通常就是 `菜单`。

### Q3：短代码不生效？

检查：

- 是否使用 `原生MD()` 或 `发MD()`，不是 `文字()`。
- Markdown 链接格式是否正确。
- `command=` 后面的指令不要带多余空格。

### Q4：按钮回调收不到？

检查：

- 事件是否为 `INTERACTION_CREATE`。
- 插件是否允许 `消息来源 == "互动"`。
- 回调数据优先从：

```php
raw["d"]["data"]["resolved"]["button_data"]
```

取，不存在再看：

```php
raw["d"]["data"]["data"]
```

### Q5：保存插件后页面提示失败但内容写入了？

`admin/api/plugin.php` 的 `write` 当前使用 `file_put_contents` 返回值判断；如果写入内容为空字符串或写入 0 字节，可能被当成失败。正常插件源码不应为空。

### Q6：如何调试接口返回？

```php
$res = curl($url, "GET", [], "");
wlog("接口返回：" . $res);
```

---

## 15. 开发注意事项

- 插件必须以 `<?php` 开头。
- 不建议写闭合 `?>`，避免多余空白输出。
- 不要在插件中 `echo`、`var_dump`、`print_r` 到页面。
- 使用 `wlog()` 调试。
- 网络接口建议设置好异常兜底。
- 不要写死循环、长时间 `sleep()`，会阻塞消息处理。
- 插件之间同一请求会顺序执行；一个插件回复后如果不 `return`，当前插件后续逻辑还会继续跑。
- 多插件可能同时响应同一条消息，开发时要做好命令过滤。
- 重要数据使用 `读()` / `写()`，不要直接乱写框架核心文件。
- 删除、覆盖插件前先备份。
